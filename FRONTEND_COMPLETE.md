# 记忆外挂 · 微信小程序前端全部完成

**时间**: 2026-06-08 10:05-10:20 GMT+8
**状态**: 前端代码全部写入完毕，31个文件，~1700行代码

## 项目结构
```
miniprogram/
├── app.js              (41行) 全局初始化、API地址、GlobalData
├── app.json            (50行) 窗口配置、tabBar(5标签)、分包
├── app.wxss            (125行) 全局样式：暗色主题、通用类(.card/.tag/.empty-state等)
├── project.config.json (41行) 微信开发者工具项目配置
├── sitemap.json         (6行) 索引策略
├── utils/
│   ├── api.js          (60行) 封装10个REST接口调用
│   └── util.js         (54行) 时间格式化、实体图标、标签颜色等工具函数
├── components/
│   └── memory-card/    (4文件 155行) 记忆卡片组件：标签/实体/来源/长按操作
└── pages/
    ├── capture/        (4文件 347行) 首页：录音按住说话、打字输入、记忆列表、离线缓存
    ├── discover/       (4文件 194行) 发现：统计卡片、AI回顾、标签预览
    ├── search/         (4文件 168行) 搜索：搜索栏、历史记录、结果列表
    ├── detail/         (4文件 306行) 详情：编辑/复制/删除、关联记忆、元信息
    └── mine/           (4文件 283行) 我的：标签云、API配置、离线同步、导出
```

## 架构设计

- **组件化**: memory-card 被 capture/search 页面复用，支持长按操作(复制/编辑/删除)
- **离线优先**: 网络异常时文字记忆自动缓存到 Storage，可在「我的」页手动同步
- **标签贯通**: 卡片标签可点击→自动跳转搜索；搜索页历史自动保存10条
- **编辑模式**: 详情页支持inline编辑内容和标签
- **API层**: utils/api.js 统一封装，支持通过Storage动态配置API地址

## 暗色主题

全局统一 `.card` 卡片类（#1a1a2e背景 / #2a2a3e边框），`.tag` 彩标（基于标签名hash取色），页脚 `.action-bar` fixed操作栏

## 后续待做

1. tabbar图标PNG需用户提供或手动添加（app.json中已占位）
2. 微信同声传译插件接入（目前语音转文字功能为占位）
3. 在微信开发者工具中打开 project.config.json 配置的 appid 验证
4. 后端需启动 memory-app 服务对接
